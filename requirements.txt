yt-dlp[default,curl-cffi]
curl-cffi
pycryptodome
appdirs
py7zr
rich>=13.0.0
easygui
questionary
requests
cloudscraper
toml
beautifulsoup4>=4.12.0
# NOT: Pillow / pypresence / selenium buradan çıkarıldı (Faz 9).
#   Pillow   → yalnızca eski CustomTkinter GUI'si (gui/main.py) kullanıyordu.
#   selenium → yalnızca eski Selenium cookie tarayıcısı; Qt karşılığı
#              QtWebEngine kullanıyor, ek paket gerekmiyor.
#   pypresence → yalnızca GUI'de (opsiyonel) → requirements-gui.txt
# Üçü de koşulsuz kuruluyordu; CLI kullanıcısı bedelini boşuna ödüyordu.
# Baslik eslestirmesinin KALITESINI belirliyor, opsiyonel degil:
# yoksa title_match SequenceMatcher yedegine duser ve AniList
# eslestirmesi "Frieren" gibi kisaltilmis adlarda None doner.
rapidfuzz>=3.9
