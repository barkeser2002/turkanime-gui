Flask==3.0.3
Flask-CORS==4.0.1
mysql-connector-python==9.0.0
requests==2.32.3
curl_cffi==0.7.4
beautifulsoup4==4.12.3
rapidfuzz==3.9.7

# Aşağıdakiler API sunucusunun değil, tarayıcı/yayıncı profillerinin
# bağımlılığı. Tarayıcı `turkanime_api.sources.*` adaptörlerini kullanıyor;
# `turkanime_api/__init__.py` daha ilk satırında `objects` (→ yt_dlp) ve
# `bypass` (→ appdirs, Crypto) çekiyor. Üçü olmadan konteyner ImportError ile
# anında çıkıyor, arşiv hiç üretilmiyor ve yayıncı her turda "arsiv-eksik"
# diyordu. yt-dlp yalnızca import zinciri yüzünden gerekli — tarayıcı video
# indirmiyor, bu yüzden `[default]` extra'sı bilinçli olarak alınmıyor.
yt-dlp==2026.7.4
appdirs==1.4.4
pycryptodome==3.23.0
