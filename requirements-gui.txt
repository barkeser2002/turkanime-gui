-r requirements.txt
# PySide6 (LGPL) — QtWebEngine dahil. Yeni Qt GUI'si bunun üzerine kuruluyor.
PySide6>=6.7
# NOT: customtkinter, CTk->Qt geçişi tamamlanana kadar korunuyor (eski GUI çalışır kalsın).
# Geçiş tamamlandığında (A5) kaldırılacak.
customtkinter>=5.2.0
toml
