-r requirements.txt
# PySide6 (LGPL) — QtWebEngine dahil. GUI bunun üzerine kuruluyor.
PySide6>=6.7
# Discord Rich Presence: yalnızca GUI kullanıyor (`gui/qt/discord.py`) ve orada
# da import hatası tolere ediliyor — bu yüzden CLI'ya değil GUI'ye bağlı.
pypresence
toml
